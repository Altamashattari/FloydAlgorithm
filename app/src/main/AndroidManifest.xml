<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.floydalgorithm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/java"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".runAlgo" />
        <activity android:name=".takeEdges" />
        <activity android:name=".Floyd" />
        <activity android:name=".Floyd1" />
        <activity android:name=".Intro" />
        <activity android:name=".problem_stat" />
        <activity android:name=".pseudocode" />
        <activity android:name=".Applications" />
        <activity android:name=".example"></activity>
    </application>

</manifest>